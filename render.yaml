services:
  - type: web
    name: fudd-backend
    env: python
    buildCommand: "cd fudd-backend && pip install -r requirements.txt"
    startCommand: "cd fudd-backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
